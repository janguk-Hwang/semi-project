<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UsersMapper">
    <insert id="insertUser" parameterType="UsersDto">
        <selectKey keyProperty="member_id" resultType="int" order="BEFORE">
            select seq_users.nextval from dual
        </selectKey>
        insert into users(member_id,id,pwd,email,name,phone,sign_in_date,role,gender,addr)
            values(#{member_id},#{id},#{pwd},#{email},#{name},#{phone},sysdate,#{role},#{gender},#{addr})
    </insert>
    <select id="selectOne" parameterType="String" resultType="UsersDto">
        select * from users where id=#{value}
    </select>
    <select id="isMembers" resultType="UserDto" parameterType="UserDto">
        select * from users where id=#{id} and pwd=#{pwd}
    </select>
</mapper>